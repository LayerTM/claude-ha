# Pinned to the current Home Assistant release that
# pytest-homeassistant-custom-component tracks (HA 2026.9.2 on Python 3.14).
# Its homeassistant pin decides every component requirement below it.
pytest-homeassistant-custom-component==0.13.365

# The requirements of the Home Assistant components this integration depends on
# (conversation, frontend, ...) are not listed here: Home Assistant pins them in
# its own manifests, and scripts/install_component_requirements.py installs
# exactly those after this file.

# Lint + type-check tooling. This file is the only place the ruff version is
# written down: CI installs it from here, the pre-commit hook runs whatever ruff
# the environment has, and CI fails if a second pin reappears (see
# .github/workflows/lint.yml).
ruff==0.16.8
mypy==2.3.1
# Type stubs for PyYAML (conversation.py renders drafted automations as YAML).
types-PyYAML==6.0.12.20260906
