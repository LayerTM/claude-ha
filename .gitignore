__pycache__/
*.py[cod]
*.egg-info/
.venv*/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
dist/
build/
.DS_Store
.idea/
.vscode/

# tldr code-analysis tool cache — local only
.tldr/
.tldrignore
.claude/

# local build notes (verified 2026 HA API research) — not shipped
.research/

# local-only working directory — never tracked
.maestri/

# local-only review correspondence — never tracked
.team-comm/

# byproduct of installing requirements_test.txt with `uv pip`. Dependencies are
# pinned in requirements_test.txt, which is what CI installs from, so a lock file
# here would be a second source of truth that nothing reads.
uv.lock
