name: hassfest

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "30 4 * * *"

permissions:
  contents: read

jobs:
  hassfest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: home-assistant/actions/hassfest@master
